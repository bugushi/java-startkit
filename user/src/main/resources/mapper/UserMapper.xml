<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.user.dao.UserMapper">
  <resultMap id="BaseResultMap" type="com.example.demo.user.entity.UserEntity">
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="user_name" jdbcType="VARCHAR" property="userName" />
    <result column="password" jdbcType="VARCHAR" property="password" />
    <result column="salt" jdbcType="VARCHAR" property="salt" />
  </resultMap>
  <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer">
    delete from user
    where id = #{id,jdbcType=INTEGER}
  </delete>
  <insert id="insert" parameterType="com.example.demo.user.entity.UserEntity">
    insert into user (id, user_name, password,
      salt)
    values (#{id,jdbcType=INTEGER}, #{userName,jdbcType=VARCHAR}, #{password,jdbcType=VARCHAR},
      #{salt,jdbcType=VARCHAR})
  </insert>
  <update id="updateByPrimaryKey" parameterType="com.example.demo.user.entity.UserEntity">
    update user
    set user_name = #{userName,jdbcType=VARCHAR},
      password = #{password,jdbcType=VARCHAR},
      salt = #{salt,jdbcType=VARCHAR}
    where id = #{id,jdbcType=INTEGER}
  </update>
  <select id="selectByPrimaryKey" parameterType="java.lang.Integer" resultMap="BaseResultMap">
    select id, user_name, password, salt
    from user
    where id = #{id,jdbcType=INTEGER}
  </select>
  <select id="selectAll" parameterType="com.example.demo.user.vo.query.UserQuery" resultMap="BaseResultMap">
    select id, user_name, password, salt
    from user
    <where>
      <if test="userName != null">
        and user_name = #{userName, jdbcType=VARCHAR}
      </if>
    </where>
    <if test="offset != null">
      limit ${offset}, ${pageSize}
    </if>
  </select>
  <select id="selectCount" parameterType="com.example.demo.user.vo.UserVO" resultType="long">
    select count(0) from user
    <where>
      <if test="userName != null">
        and user_name = #{userName, jdbcType=VARCHAR}
      </if>
    </where>
  </select>
</mapper>